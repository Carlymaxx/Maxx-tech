<!--
File: maxx-bot-robot.html
Purpose: "Maxx Bot — Futuristic Neon Edition" — full standalone HTML + CSS with SVG animation.
Instructions:
 - To preview: open this file in a browser.
 - To include in README.md: copy the SVG block between <!-- SVG START --> and <!-- SVG END --> and paste into your README.md (GitHub supports inline SVG in markdown).The SVG is self-contained and uses CSS animations for blinking, floating, alternating arm waves, slow chest-light spin, antenna pulse sync, reflective floor glow, and a stylized neon-flicker "Maxx Bot" text that gently floats. -->


<!doctype html>

<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Maxx Bot — Futuristic Neon Edition</title>
  <style>
    :root{
      --bg-1:#020615;          /* deep dark */
      --bg-2:rgba(2,12,35,0.85);/* faint blue aura */
      --neon-1:#00e0ff;        /* neon blue */
      --neon-2:#00a8ff;        /* secondary neon */
      --panel:#021821;
      --shadow: rgba(0,0,0,0.6);
    }html,body{height:100%;margin:0;background:radial-gradient(circle at 50% 35%, rgba(0,160,255,0.06), transparent 18%), linear-gradient(180deg,var(--bg-1), #000814);display:grid;place-items:center;font-family:Inter,system-ui,Arial,Helvetica,sans-serif;color:#eaffff}
.frame{width:min(920px,94vw);padding:28px;box-sizing:border-box}
.card{width:100%;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));border-radius:18px;padding:26px;box-shadow:0 18px 40px var(--shadow);display:grid;grid-template-columns:1fr 360px;gap:22px;align-items:center}
.preview{display:flex;align-items:center;justify-content:center;padding:10px}
.meta{padding:10px}
h1{margin:0;font-size:20px;letter-spacing:0.6px}
p{margin-top:10px;color:rgba(234,255,255,0.92);line-height:1.35}
.btn{margin-top:14px;display:inline-block;padding:8px 12px;border-radius:10px;background:linear-gradient(90deg,var(--neon-1),var(--neon-2));color:#001; font-weight:700;text-decoration:none}

svg.robot-svg{width:100%;height:auto;max-width:420px}

/* === Animations (for the HTML preview wrapper) === */
@keyframes float { 0%,100%{transform:translateY(0)} 50%{transform:translateY(-7px)} }

/* The following CSS classes are mirrored inside the SVG for README compatibility, but keep here for live preview fallback */
.robot-wrap{animation:float 3.5s ease-in-out infinite}

@media (max-width:900px){
  .card{grid-template-columns:1fr;}
  .meta{text-align:center}
}

  </style>
</head>
<body>
  <div class="frame">
    <div class="card">
      <div class="preview"><!-- SVG START -->
    <!-- Copy everything from this comment to the 'SVG END' comment into a README.md to render inline on GitHub. -->
    <svg class="robot-svg" viewBox="0 0 420 420" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Maxx Bot - Futuristic Neon Edition">
      <style>
        /* embedded SVG styles ensure README rendering keeps animations */
        :root{ --neon-1: #00e0ff; --neon-2:#00a8ff; --bg-faint: rgba(0,160,255,0.06); }

        .wrap{filter:url(#drop);}

        /* float the whole robot */
        @keyframes svggfloat { 0%,100%{transform:translateY(0)} 50%{transform:translateY(-8px)} }

        /* blink for eyes */
        @keyframes blink { 0%,90%,100%{transform:scaleY(1)} 95%{transform:scaleY(0.12)} }

        /* arm waving: left and right out-of-phase */
        @keyframes waveA { 0%,100%{transform:rotate(6deg)} 50%{transform:rotate(-40deg)} }
        @keyframes waveB { 0%,100%{transform:rotate(-6deg)} 50%{transform:rotate(40deg)} }

        /* chest spin slow */
        @keyframes slowspin { 0%{transform:rotate(0deg)} 100%{transform:rotate(360deg)} }

        /* antenna pulse synced */
        @keyframes pulse { 0%{opacity:1; transform:scale(1)} 50%{opacity:0.45; transform:scale(0.85)} 100%{opacity:1; transform:scale(1)} }

        /* neon flicker for text */
        @keyframes neonflicker { 0%{opacity:0.95; filter:drop-shadow(0 0 12px rgba(0,224,255,0.8))} 50%{opacity:0.6; filter:drop-shadow(0 0 6px rgba(0,160,255,0.45))} 100%{opacity:0.95; filter:drop-shadow(0 0 12px rgba(0,224,255,0.8))} }

        .robot-wrap{animation:svggfloat 3.6s ease-in-out infinite; transform-origin:200px 200px}

        .eye-shade{transform-origin:center; animation:blink 4.6s infinite}

        /* arms: pivot near shoulder */
        .arm-l{transform-origin:107px 175px; animation:waveA 2.8s ease-in-out infinite}
        .arm-r{transform-origin:292px 175px; animation:waveB 2.8s ease-in-out infinite}

        /* chest lights group rotates slowly */
        .chest-rot{transform-origin:200px 220px; animation:slowspin 12s linear infinite}

        /* antenna pulse synced to chest: slightly faster phase-offset to feel alive */
        .antenna-pulse{transform-origin:200px 44px; animation:pulse 6s ease-in-out infinite}

        /* subtle floor reflection pulse (sync) */
        .floor-glow{animation:pulse 6s ease-in-out infinite}

        /* neon text flicker and float */
        .neon-text{animation:neonflicker 3.6s ease-in-out infinite; transform-origin:200px 340px}
        .neon-text-float{animation:svggfloat 3.6s ease-in-out infinite}

        /* small ease for hover (preview) */
        svg:hover .arm-l, svg:hover .arm-r { animation-duration:2.2s }
      </style>

      <defs>
        <linearGradient id="g1" x1="0" x2="1">
          <stop offset="0" stop-color="var(--neon-1)" />
          <stop offset="1" stop-color="var(--neon-2)" />
        </linearGradient>

        <radialGradient id="gA" cx="50%" cy="35%">
          <stop offset="0%" stop-color="#00bfff" stop-opacity="0.12" />
          <stop offset="100%" stop-color="#000000" stop-opacity="0" />
        </radialGradient>

        <filter id="drop" x="-50%" y="-50%" width="200%" height="200%">
          <feDropShadow dx="0" dy="18" stdDeviation="20" flood-color="#001022" flood-opacity="0.6"/>
        </filter>

        <filter id="glow" x="-50%" y="-50%" width="200%" height="200%">
          <feGaussianBlur stdDeviation="4" result="b"/>
          <feMerge>
            <feMergeNode in="b"/>
            <feMergeNode in="SourceGraphic"/>
          </feMerge>
        </filter>
      </defs>

      <!-- faint blue aura background -->
      <rect x="0" y="0" width="420" height="420" fill="url(#gA)" />

      <g class="wrap robot-wrap">
        <!-- body glow circle -->
        <circle cx="200" cy="180" r="120" fill="url(#g1)" opacity="0.08" />

        <!-- body -->
        <rect x="120" y="150" rx="22" ry="22" width="160" height="150" fill="url(#g1)" stroke="#00151a" stroke-width="4" />

        <!-- chest panel (rotating element) -->
        <g class="chest-rot" transform="translate(0,0)">
          <rect x="150" y="185" width="100" height="70" rx="10" fill="#021821" />

          <!-- spinning ring of lights (will rotate) -->
          <g transform="translate(200,220)">
            <circle r="26" fill="none" stroke="#052a2e" stroke-width="6" />
            <g id="lights">
              <circle cx="0" cy="-22" r="4" fill="var(--neon-1)" />
              <circle cx="15" cy="-15" r="4" fill="var(--neon-2)" />
              <circle cx="22" cy="0" r="4" fill="var(--neon-1)" />
              <circle cx="15" cy="15" r="4" fill="var(--neon-2)" />
              <circle cx="0" cy="22" r="4" fill="var(--neon-1)" />
              <circle cx="-15" cy="15" r="4" fill="var(--neon-2)" />
              <circle cx="-22" cy="0" r="4" fill="var(--neon-1)" />
              <circle cx="-15" cy="-15" r="4" fill="var(--neon-2)" />
            </g>
          </g>
        </g>

        <!-- head -->
        <rect x="140" y="80" rx="20" ry="20" width="120" height="90" fill="url(#g1)" stroke="#00151a" stroke-width="4" />

        <!-- eyes -->
        <g transform="translate(170,105)">
          <rect x="0" y="0" rx="6" width="20" height="14" fill="#00121a" />
          <rect x="44" y="0" rx="6" width="20" height="14" fill="#00121a" />
          <g class="eye-shade">
            <circle cx="10" cy="7" r="4" fill="var(--neon-1)" />
            <circle cx="54" cy="7" r="4" fill="var(--neon-1)" />
          </g>
        </g>

        <!-- mouth -->
        <rect x="170" y="132" rx="6" width="60" height="10" fill="#00121a" opacity="0.95" />

        <!-- antenna (pulse) -->
        <g class="antenna-pulse">
          <line x1="200" y1="68" x2="200" y2="48" stroke="var(--neon-1)" stroke-width="4" stroke-linecap="round"/>
          <circle cx="200" cy="44" r="7" fill="var(--neon-1)" filter="url(#glow)" />
        </g>

        <!-- left arm (waving) -->
        <g class="arm-l">
          <rect x="95" y="170" rx="12" width="25" height="90" fill="url(#g1)" stroke="#00151a" stroke-width="3"/>
          <circle cx="107" cy="268" r="12" fill="#00121a" />
        </g>

        <!-- right arm (waving opposite phase) -->
        <g class="arm-r">
          <rect x="280" y="170" rx="12" width="25" height="90" fill="url(#g1)" stroke="#00151a" stroke-width="3"/>
          <circle cx="292" cy="268" r="12" fill="#00121a" />
        </g>

        <!-- legs -->
        <rect x="150" y="300" rx="10" width="30" height="38" fill="#00121a"/>
        <rect x="220" y="300" rx="10" width="30" height="38" fill="#00121a"/>

        <!-- details -->
        <rect x="128" y="160" width="12" height="8" rx="3" fill="#00121a"/>
        <rect x="260" y="160" width="12" height="8" rx="3" fill="#00121a"/>

        <!-- floor reflection (soft) -->
        <ellipse class="floor-glow" cx="200" cy="362" rx="95" ry="20" fill="url(#g1)" opacity="0.07" filter="url(#glow)" />

        <!-- neon stylized text (floating + flicker) -->
        <g class="neon-text neon-text-float" transform="translate(200,38)" text-anchor="middle">
          <text x="0" y="325" font-family="Inter, Arial, Helvetica, sans-serif" font-size="36" font-weight="700" fill="#ecfeff" style="letter-spacing:2px">Maxx</text>
          <text x="0" y="358" font-family="Inter, Arial, Helvetica, sans-serif" font-size="18" font-weight="700" fill="var(--neon-2)" style="letter-spacing:1px">Bot</text>
          <!-- glowing overlay for neon effect -->
          <text x="0" y="325" font-family="Inter, Arial, Helvetica, sans-serif" font-size="36" font-weight="700" fill="var(--neon-1)" opacity="0.22" style="letter-spacing:2px; filter:drop-shadow(0 0 12px var(--neon-1)); animation:neonflicker 3.6s ease-in-out infinite"></text>
        </g>

      </g>

      <!-- To ensure rotation of chest lights uses transform from chest center, add a small script-free trick: animate the group with CSS chest-rot above. -->
    </svg>
    <!-- SVG END -->

  </div>

  <div class="meta">
    <h1>Maxx Bot — Futuristic Neon Edition</h1>
    <p>All done! This file contains a fully animated, neon-blue Maxx Bot. To add it to your GitHub README:
      <ol>
        <li>Open this file in the canvas and copy the SVG block (from the &lt;!-- SVG START --&gt; to &lt;!-- SVG END --&gt; comment).</li>
        <li>Paste that SVG markup directly into your README.md. GitHub will render it inline.</li>
        <li>If you prefer an image file (PNG), open this HTML in a browser, take a screenshot or export as PNG using a tool, and include it in your repo with standard Markdown image syntax.</li>
      </ol>
    </p>
    <a class="btn" href="#svg">Copy SVG for README</a>
    <p style="margin-top:10px;font-size:13px;opacity:0.9">Want alternate color themes, different text, or a PNG export I can create for you? Tell me which one.</p>
  </div>
</div>

  </div>
</body>
</html>